services:
  - type: web
    name: backend
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: uv sync --frozen
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
