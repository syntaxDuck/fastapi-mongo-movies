services:
  - type: web
    name: backend
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT 
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
